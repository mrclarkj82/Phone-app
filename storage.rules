rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Storage is intentionally closed for every app in the shared project.
    // Student evidence uses approved links rather than direct file uploads.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
